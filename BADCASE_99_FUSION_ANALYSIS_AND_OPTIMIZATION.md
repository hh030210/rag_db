# 融合检索实验 99 条 Badcase 原因分析与整体优化方案

## 1. 分析范围与实验条件

本报告分析融合检索问答实验中经过 Qwen-plus 二次复核的 `99` 条有效 badcase。空问题不计入本报告。该实验在同一份 `436` 条测试集上运行，实际条件为：

- 检索模式：语义检索与维度检索融合；
- 最终 Top-K：`5`；
- 融合策略：自适应权重 + 得分重排；
- 问答模型：Qwen-plus；温度：`0.1`；最大生成长度：`512`。

这 `99` 条是“模型二次复核后的综合 badcase”，不是人工标注金标准。二次复核同时给出答案判定、检索判定和综合判定，主错误类型是互斥分类。

## 2. 99 条 Badcase 的总体分布

| 主错误类型 | 数量 | 占 99 条的比例 | 说明 |
|---|---:|---:|---|
| 答案不完整 | 43 | 43.434% | 已回答部分问题，但遗漏问题明确要求的事实、年份、数量或列表项 |
| 检索未命中 | 36 | 36.364% | Top-K 没有提供回答问题所需的支持性证据 |
| 事实或内容错误 | 16 | 16.162% | 答案与证据矛盾、实体混淆或关键事实错误 |
| 应答但拒答 | 4 | 4.040% | 上下文足以回答，但系统错误输出“文中未说明”等拒答语 |
| 合计 | 99 | 100.000% | — |

这次最突出的变化是：答案不完整成为第一大问题，而拒答已经明显减少。上下文从每个 chunk 前 `500` 字符增加到 `2000` 字符后，模型能看到更多证据，因此“完全拒答”和部分事实错误有所下降；但由于本次同时把检索从语义模式改成融合模式，不能把下降全部归因于上下文长度变化。

## 3. 检索问题与答案问题的交叉分析

| 主错误类型 | 检索命中 | 检索未命中 | 主要责任判断 |
|---|---:|---:|---|
| 答案不完整 | 37 | 6 | 主要是生成阶段没有覆盖问题要求 |
| 检索未命中 | 0 | 36 | 主要是召回、实体识别或排序问题 |
| 事实或内容错误 | 14 | 2 | 多数情况下证据已召回，但模型使用错误 |
| 应答但拒答 | 4 | 0 | 全部是生成策略或证据利用问题 |
| 合计 | 55 | 44 | — |

在 `99` 条综合 badcase 中：

- `43` 条是“答案判定 badcase 且检索命中”，属于明确的证据已到位但答案生成错误；
- `12` 条是“答案判定 uncertain 且检索命中”，需要人工重点确认；
- `43` 条同时表现为答案错误和检索未命中；
- `1` 条答案本身可接受，但检索未命中，因此仍被综合判为 badcase。

因此，当前 `55` 条检索命中后的综合 badcase 不能靠单纯提高召回率解决；其中至少 `43` 条明确需要优化答案生成、事实核验和完整性检查。


## 4. 四类 Badcase 的原因分析

### 4.1 答案不完整：43 条

其中 `37` 条检索已经命中，说明主要不是知识库没有内容，而是模型没有完成问题要求。典型原因包括：

1. 没有识别问题中的多个回答槽位。例如“分别回答两个景点”“至少列举三类”“说明时间和特色”等复合要求没有被拆开。
2. 只回答了问题的第一部分，遗漏比较对象、历史背景、活动安排或交通看点。
3. 标准答案包含多个事实，但模型没有判断哪些是问题明确要求的必答内容。
4. 证据分布在多个 chunk，模型没有对多段证据进行汇总。
5. 当前 Prompt 虽然要求“列举、分别、至少 N 项”按条目回答，但没有生成后检查机制，模型漏答后仍会直接返回。

例如，问题要求说明恢复祭祀时间和祭孔典礼特色，系统可能只回答其中的时间或只描述典礼形式；这不是单纯的文本长度问题，而是缺少问题要求清单。

优化重点是“问题要求解析 + 必答项校验”，而不是继续无限增加上下文长度。

### 4.2 检索未命中：36 条主错误，44 条检索 miss

当前检索未命中的核心表现是“返回了结果，但结果不支持问题”，不是 Qdrant 为空。实验中空检索为 `0`，说明主要是召回内容相关性不足。典型原因包括：

1. 查询扩展关闭，比较、路线、多景点和多实体问题仍被压缩成一个整体查询。
2. 景点别名、建筑别名和历史名称没有统一，专有名词容易被相似词带偏。
3. Top-K=`5` 时，相关 chunk 可能已经进入候选集，但未进入最终上下文。
4. 维度检索和语义检索的候选深度不同，融合前后的分数尺度不一致，可能造成某一路过度主导排序。
5. 多段证据需要联合检索，但当前融合单位仍然是 chunk，缺少按实体或子问题的证据覆盖约束。

例如，询问“棂星门为什么又叫龙凤门和火焰牌坊”时，Top-K 返回西湖、龙门石窟和颐和园内容，说明应优先解决专有名词召回和实体约束，而不是让生成模型自行猜测。

### 4.3 事实或内容错误：16 条

其中 `14` 条检索已经命中，表明当前主要问题是证据利用和事实核验。典型原因包括：

- 证据中已有明确数字、年份或票务条件，但模型没有复制或错误改写；
- 两个相似景点、建筑或人物的信息发生混淆；
- 对“免票”“优惠”“年龄条件”等否定和条件关系理解错误；
- 当证据表达为“据传”“尚无定论”时，模型把不确定事实回答成绝对结论；
- 没有为每个答案要点保留来源，导致无依据内容混入最终答案。

这类问题应增加“答案要点—证据片段”对应关系，并对数字、年份、价格、年龄和否定句进行规则核验。

### 4.4 应答但拒答：4 条

本次只有 `4` 条，且全部检索命中，说明把上下文长度从 `500` 提高到 `2000` 后，拒答问题已经显著减少。但剩余问题说明拒答逻辑仍可能过于保守，尤其在以下场景中容易发生：

- 答案需要把多个 chunk 合并后才能得到；
- 问题使用了口语或别名，证据使用了正式名称；
- 上下文没有一句话直接回答，但可以由两条证据进行有限归纳。

改进方向不是取消拒答，而是增加证据充分性判断：证据能够直接支持或通过简单组合支持时回答；证据确实不足时才拒答。

## 5. 当前逻辑的主要缺陷

### 5.1 评测数据不够结构化

当前 `99` 条记录的问题类型均为 `unknown`，景区字段为空，标准证据 ID 没有提供，全部记录还被解析为可回答问题。这导致无法可靠分析不同问题类型的失败率，也无法严格判断某个 gold chunk 是否被召回。

建议为每道题补充：

- `question_type`：单实体、多实体、比较、列表、时间、票务、路线、不可回答等；
- `spot`：景区、建筑或主要实体；
- `answerable`：是否可由知识库回答；
- `gold_chunk_ids`：标准证据对应的精确 chunk ID；
- `required_facts`：问题要求的事实槽位；
- `acceptable_answers`：语义等价或表达变体。

### 5.2 自动 badcase 规则更适合预筛选

本次有效自动候选为 `271` 条，但只有 `99` 条被模型二次复核判定为综合 badcase，说明自动规则仍有大量候选需要排除。当前数字缺失、token F1 和文本近似命中规则不能直接替代事实级判断。

尤其是“参考答案有数字但系统答案没有数字”的规则，必须先判断问题是否真的要求该数字；否则会把回答了问题核心但没有补充附加细节的答案误报为 badcase。

### 5.3 融合检索还没有证明提升了召回

当前融合实验与之前语义实验的全量自动 Hit@5 都是 `272/435`。这说明需要查看融合前后的 gold chunk 排名，而不是只看最终综合 badcase 数量。下一步必须报告 Recall@5、Recall@10、MRR 和 gold chunk 平均排名。

### 5.4 融合分数需要统一尺度

当前两路检索的候选数量和原始排序深度不同。若直接使用与候选列表长度相关的 rank score，再用自适应权重相加，可能使两路分数的实际影响与设定权重不一致。

应选择以下一种稳定方案：

1. 两路先统一召回深度，再进行 RRF；
2. 两路分数分别做 min-max 或 z-score 归一化后再加权；
3. 使用验证集学习融合权重，而不是仅依赖启发式自适应权重；
4. 增加“实体覆盖”和“问题槽位覆盖”作为重排约束。

### 5.5 生成结果仍缺少后处理验证

即使上下文已经增加到 `2000` 字符，仍有 `43` 条答案不完整、`16` 条事实错误。当前流程生成后直接返回，没有逐项检查实体、数字、年份、数量和比较关系。

## 6. 整体优化方案

### 6.1 P0：先修复数据和评测口径

1. 为问题建立必答事实槽位，并区分问题要求与标准答案附加信息。
2. 建立精确 gold chunk ID，优先按 ID 计算检索命中。
3. 为不可回答问题补充标注，单独评测拒答准确率。
4. 将 LLM 二次复核作为辅助标注，人工抽查 uncertain 和边界样本。
5. 将答案正确性拆为事实正确、要求完整、证据支持和格式满足四个子指标。

### 6.2 P1：增加问题分析和证据覆盖层

1. 先从问题中抽取实体、关系、数量、时间和约束词。
2. 对“分别、比较、至少、列举、多少、何时、是否”等词生成必答项清单。
3. 对多实体问题按实体拆分检索，对多目标问题按目标拆分检索。
4. 融合各子问题证据，并要求最终上下文覆盖全部必答项。
5. 对专有名词建立别名表、同义词表和实体元数据过滤。

### 6.3 P2：改进融合检索和排序

1. 候选召回阶段使用 Top-20 或 Top-50，最终问答上下文再筛选 Top-5 或 Top-8。
2. 对语义路和维度路统一候选深度、分数尺度和去重规则。
3. 用 RRF 或归一化分数替换未经校准的直接 rank 加权。
4. 加入实体一致性、文档多样性和问题槽位覆盖得分。
5. 对相关 chunk 的父 chunk、相邻 chunk 或同文档摘要做受控扩展，避免答案跨 chunk 时只召回半段信息。
6. 对比较、路线和组合问题增加多路检索合并，而不是只依赖一个整体 query。

### 6.4 P3：改进答案生成和事实校验

1. 保留 `2000` 字符上下文作为候选配置，但通过消融实验确认其收益，不继续盲目增大。
2. Prompt 先要求模型内部建立必答项清单，再按问题顺序逐项回答。
3. 对“分别”“至少 N 项”和列表问题使用强制条目格式。
4. 生成答案后检查所有实体、数字、年份、价格、年龄条件和否定关系。
5. 为每个答案要点绑定来源 chunk；无法找到来源的要点标记为 unsupported。
6. 校验失败时进行一次定向重生成，只提示缺失项和对应证据，避免无目标地重复生成。
7. 对证据不足和证据充分分别设置拒答策略，防止“禁止编造”退化成无依据拒答。

### 6.5 P4：建立可解释的实验报告

每次实验至少报告：

- 检索 Recall@5、Recall@10、MRR、nDCG；
- 答案事实正确率、完整性、证据支持率和拒答准确率；
- 综合 badcase 率及四类主错误比例；
- 不同问题类型、景区和实体的分组结果；
- 单因素消融结果和配对显著性检验。



## 8. 结论

当前最优先的工作不是继续调整单一融合权重，而是：

1. 为问题建立必答事实槽位和精确证据标注；
2. 通过四组单因素消融区分融合检索和上下文长度的真实贡献；
3. 在生成后增加答案完整性、事实一致性和证据支持校验；
4. 对多实体、比较、列表和路线问题实施查询分解与证据覆盖检索。

完整逐条复核记录见 [融合检索 99 条有效 badcase](/Users/a1234/RAG_DB_silm/results/server0/badcase_fusion_context2000_20260901_reviewed_badcases_valid.jsonl)，可读版见 [融合检索 badcase 可读版](/Users/a1234/RAG_DB_silm/results/server0/badcase_fusion_context2000_20260901_reviewed_badcases_readable.json)。
