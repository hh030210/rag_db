# 136 条综合 Badcase 原因分析与优化方案

## 1. 分析范围

本报告针对景区问答数据集中的 `436` 条记录进行分析。原始数据中有 `435` 条有效问题和 `1` 条空问题；自动规则筛选出 `283` 条候选记录，并使用 Qwen-plus 对候选记录进行二次复核。

最终有 `136` 条被模型判定为综合 badcase，另有 `146` 条可接受、`1` 条不确定。需要注意，以下 `136` 条是模型辅助复核结果，不是人工标注金标准。

本次实验的实际配置是：语义检索、Top-K=`5`、关闭查询扩展、Qwen-plus、温度 `0.1`、最大生成长度 `512`。因此，下面的结论首先反映当前“语义检索 + 当前问答生成逻辑”的表现，不能直接等同于三阶段分片方法在所有配置下的最终能力。

## 2. Badcase 总体分类

136 条综合 badcase 按互斥的主错误类型分类如下：

| 主错误类型 | 数量 | 占 136 条的比例 | 主要含义 |
|---|---:|---:|---|
| 答案不完整 | 44 | 32.353% | 回答方向基本正确，但遗漏问题明确要求的年份、数量、对象或列表项 |
| 检索未命中 | 33 | 24.265% | Top-K 没有提供回答所需的支持性证据 |
| 事实或内容错误 | 31 | 22.794% | 答案与证据矛盾、出现错误事实、实体混淆或关键事实错误 |
| 应答但拒答 | 28 | 20.588% | 上下文已经足以回答，但模型错误地输出“文中未说明”等拒答语 |
| 合计 | 136 | 100.000% | — |

从独立检索标签看，这 136 条中有 `38` 条被判定为检索未命中；其中 `5` 条因为答案不完整、事实错误或拒答表现更突出，被归入其他主错误类型。因此，“主错误类型中的检索未命中”是 `33` 条，而“检索标签未命中”是 `38` 条，两者不能混用。

## 3. 检索与生成的责任边界

将主错误类型与检索结果交叉后，可以看到问题主要集中在生成阶段：

| 主错误类型 | 检索命中 | 检索未命中 | 结论 |
|---|---:|---:|---|
| 答案不完整 | 42 | 2 | 绝大多数情况下证据已经召回，主要是模型没有覆盖问题要求 |
| 检索未命中 | 0 | 33 | 主要是召回或排序问题 |
| 事实或内容错误 | 28 | 3 | 多数情况下上下文已有证据，但模型使用错误或混淆了事实 |
| 应答但拒答 | 28 | 0 | 全部属于证据已召回但生成策略错误 |
| 合计 | 98 | 38 | — |

因此，`98` 条 badcase 的检索结果已经命中证据，说明它们至少不是单纯的召回失败；其中 `84` 条已经明确表现为答案层面的错误，另有 `14` 条答案层面不确定但综合判为 badcase。这部分问题不能通过单纯提高召回率解决，必须增加答案约束、要点检查和生成后验证。

## 4. 各类 Badcase 的具体原因

### 4.1 答案不完整：44 条

这是数量最多的一类，占 `32.353%`。典型表现是：

- 问题要求“分别回答”多个对象，系统只回答了其中一部分；
- 问题要求具体年份、数量或时间，系统只给出笼统的朝代、类别或范围；
- 问题要求“至少三类”“列举若干项”，系统虽然给出相关内容，但没有满足数量要求；
- 证据出现在检索结果中，但由于上下文截断或模型注意力不足，没有被写入最终答案。

例如，问题询问长陵和定陵分别属于哪位皇帝、建于哪个朝代，系统回答了皇帝和“明朝”，但没有保留标准答案中的具体始建年份。这里暴露出两个问题：一是答案没有按问题中的多个要求逐项核对；二是当前评测容易把标准答案中的附加细节误认为必答内容。

### 4.2 检索未命中：33 条主错误，38 条检索未命中标签

这类问题不是没有返回结果，而是返回的 Top-K 内容与问题主题不相关，或者缺少回答问题的关键片段。典型问题包括：

- 多实体问题中，检索只关注了其中一个实体；
- 比较、路线和组合游览问题需要多个主题证据，但当前查询只形成一个整体向量；
- 景点别名、建筑名称和专有名词没有被正确识别；
- Top-K=`5` 时，相关片段排在候选列表后面；
- 查询扩展在本次批量实验中关闭，复杂问题没有被拆解为多个检索子问题。

例如，“棂星门为什么又叫龙凤门和火焰牌坊”得到的 Top-K 主要是西湖、龙门石窟和颐和园内容，说明当前问题不是生成模型不会回答，而是召回阶段没有找到棂星门相关证据。

### 4.3 事实或内容错误：31 条

这类 badcase 中有 `28` 条检索已经命中，说明错误主要发生在“证据到答案”的转换阶段，典型表现为：

- 忽略 Top-1 或 Top-K 中已经明确给出的数字、年份和票务规则；
- 将相邻景点、相似建筑或不同人物的信息混淆；
- 对否定信息、年龄条件和免票条件理解错误；
- 生成答案引用了上下文之外的常识或猜测；
- 专有名词发生错别字，进而造成实体识别错误。

例如，证据已说明“60 岁以上老人免票”，系统却回答 65 岁老人需要购票；证据已给出石牌坊建造年份，系统却回答“文中未说明”。这说明当前生成逻辑缺少证据定位和事实一致性校验。

### 4.4 应答但拒答：28 条

这 `28` 条全部检索命中，说明模型的拒答条件过于保守，或者没有正确判断上下文是否足以回答。典型表现为：

- 上下文中有直接答案，但模型仍输出“文中未说明”；
- 比较问题需要综合两段证据，模型没有完成简单归纳；
- 证据分布在不同 chunk，模型没有跨 chunk 汇总；
- Prompt 中的“不得编造”被模型错误执行成“只要不是一句原文就拒答”。

例如，检索结果已经给出昭陵和康陵的开放情况、建筑完整程度等信息，系统仍拒绝比较哪个更值得参观。这不是知识库缺失，而是回答策略问题。

## 5. 当前评测逻辑暴露的问题

### 5.1 自动候选规则误报较多

自动候选集合有 `283` 条，其中二次复核后 `146` 条被认为可接受，误报比例约为 `51.590%`。当前自动规则主要有三类：

- 参考答案中的任意数字没有出现在系统答案中，就标记为数字缺失；
- 参考答案与系统答案的 token F1 低于阈值，就标记为低重合；
- 标准证据文本与 Top-K 使用近似文本匹配判断检索命中。

这些规则适合做“候选召回”，不适合直接作为最终 badcase 判定。例如，问题只要求回答“哪个朝代”，而参考答案额外包含精确年份时，系统只回答朝代不一定是错误。后续应把自动规则定位为预筛选，并增加问题要求解析和事实槽位判断。

### 5.2 标准数据缺少结构化标注

当前 `436` 条数据都被解析为可回答问题，问题类型和景区字段没有有效填充，标准证据 ID 也没有提供，证据主要以文本形式保存。因此，目前无法可靠地按以下维度统计：

- 单实体、多实体、比较、列表、路线、时间和票务问题的 badcase 率；
- 不同景区或不同文档的检索召回差异；
- 某条答案究竟对应哪个 gold chunk；
- 复杂问题是否覆盖了全部必答事实。

这会限制论文实验的可解释性。应该为每道题补充 `question_type`、`spot`、`answerable`、`gold_chunk_ids` 和 `required_facts` 等字段。

### 5.3 当前实验配置没有覆盖完整检索方案

本次结果使用的是语义检索，查询扩展关闭，最终只向问答模型提供 5 个 chunk。虽然系统中存在维度检索和融合逻辑，但它们没有体现在这批结果中。因此，当前 136 条 badcase 不能直接证明融合检索或三阶段分片本身无效，只能说明这套实际运行配置存在问题。

### 5.4 上下文保存长度与问答使用长度不一致

评测结果中保存的检索片段最长可达到约 2000 字符，但实际生成答案时每个 chunk 只截取前 500 个字符。这样会出现“评测人员能在保存结果中看到证据，但生成模型当时没有看到证据”的情况，尤其容易造成年份、数量、展览名称和后半段列表项缺失。

### 5.5 缺少生成后校验

当前问答流程生成一次答案后直接返回，没有检查：

- 问题中的实体是否都被回答；
- “分别”“至少”“列举”“多少”“何时”等要求是否满足；
- 答案中的数字、年份和票务条件是否能在证据中找到；
- 是否在证据充分时错误拒答；
- 是否出现证据中不存在的事实。

这正是答案不完整、事实错误和拒答三类问题合计 `103` 条的共同缺口。

## 6. 按优先级的优化方案

### P0：先修正评测和数据标注

1. 为每道题建立结构化必答项。例如将“长陵和定陵分别是哪位皇帝、建于何时”拆为四个槽位：长陵墓主、长陵年份、定陵墓主、定陵年份。
2. 为每道题补充精确的 gold chunk ID，检索命中优先使用 ID 交集，文本近似匹配只作为辅助。
3. 增加问题类型和景区字段，至少覆盖单实体、多实体、比较、列表、路线、时间、票务和不可回答问题。
4. 将“标准答案中的附加信息”和“问题明确要求的信息”分开，禁止仅凭任意数字缺失就判定 badcase。
5. 将数字归一化后再比较，统一中文数字、阿拉伯数字、年份、日期和百分比的表达形式。
6. 保留 `answer_judgement`、`retrieval_judgement` 和 `overall_judgement` 三个字段，但明确综合判定规则，避免答案“不确定”却被综合判为 badcase 时无法解释。

### P1：优先解决证据已命中但答案错误的问题

1. 将每个 chunk 的生成上下文长度从 500 字符提高到 1200–2000 字符，或根据问题从 chunk 中抽取包含实体、数字和关键词的证据窗口。
2. 在 Prompt 中引入“问题要求清单”：先识别实体、数量、比较关系和时间/票务约束，再逐项作答。
3. 强制列表问题使用条目化输出；强制“分别”问题按实体一一对应；强制“至少 N 项”问题先检查数量。
4. 对数字、年份、年龄、价格和免票条件增加证据约束：答案中的关键数值必须在检索证据中出现或可由证据直接推导。
5. 生成后增加轻量答案校验器。校验不通过时，用缺失项和证据片段进行一次定向重生成，而不是直接接受第一次答案。
6. 对证据充分但模型拒答的情况增加“证据充分性判断”，只有上下文确实缺少答案时才输出拒答。

### P2：提高复杂问题的检索召回

1. 对比较、路线、多景点和多实体问题进行查询分解，每个实体或子目标单独检索，再合并证据。
2. 打开查询扩展，并与关闭扩展的结果做成配对消融实验，确认扩展是否真正降低 38 条检索未命中。
3. 将最终 Top-K 从 5 扩展测试到 8、10 和 20；候选召回保持更大范围，再使用重排模型筛选最终上下文。
4. 对景区、建筑、人名和别名增加实体识别与元数据过滤，避免“棂星门”“龙凤门”等专有名词被相似但无关的景点吸走。
5. 对同一文档的相邻 chunk 做父子或邻接扩展，防止答案的前半句和后半句被切到不同 chunk 后只召回其中一段。
6. 在维度检索与语义检索融合时，对两路分数和候选数量统一归一化，并比较固定权重、RRF 和自适应权重；不能只依据单次配置判断融合策略优劣。

### P3：补充系统级质量控制

1. 增加事实一致性检查，重点检查数字、年份、专有名词、票价、年龄条件和否定句。
2. 增加答案可追溯性，让每个答案要点关联来源 chunk；没有来源的要点自动进入复核队列。
3. 对重复问题使用唯一的样本行号或稳定样本 ID，不只使用问题 ID，避免结果覆盖或错配。
4. 将检索错误、生成错误、证据不足和输入无效分开统计，避免用一个 badcase 总数掩盖真正瓶颈。
5. 对不确定样本和边界样本建立人工抽样集，用于校准自动规则和 LLM 复核提示词。

## 7. 建议的后续实验顺序

| 顺序 | 实验 | 目的 | 主要指标 |
|---:|---|---|---|
| 1 | 当前语义检索 + Top-5 基线复现 | 固定可比较基线 | Recall@5、答案正确率、综合 badcase 率 |
| 2 | 上下文长度 500/1200/2000 对比 | 验证信息截断是否导致答案不完整 | 完整性 badcase、数字正确率、拒答率 |
| 3 | Top-5/Top-10/Top-20 对比 | 验证排序深度对检索召回的影响 | Recall@K、MRR、检索 badcase 率 |
| 4 | 查询扩展开关对比 | 验证复杂问题的查询分解价值 | 多实体/比较问题 Recall@K |
| 5 | 语义、维度、融合三路对比 | 验证三阶段方法的检索贡献 | Recall、nDCG、答案正确率 |
| 6 | 增加答案要求清单和生成后校验 | 验证生成阶段优化 | 不完整、事实错误、拒答三类 badcase |
| 7 | 组合最优配置复测 | 验证优化是否稳定 | 全量综合 badcase 率及分类型结果 |

每次实验应使用完全相同的问题集、标准答案、模型和评测规则，只改变一个因素；结果应按问题逐条配对，报告总指标和各问题类型指标，不能只比较一个总体平均值。

## 8. 结论

当前 136 条综合 badcase 中，最大问题不是单纯的“检索不到”，而是“已经检索到证据却没有正确完成答案”。其中 `98` 条检索已经命中，`84` 条明确属于检索命中后的答案问题，另有 `14` 条需要进一步人工确认；检索未命中为 `38` 条。

因此，优化顺序应当是：先修正数据和评测口径，再解决上下文截断、问题要求解析和生成后校验，最后通过查询分解、Top-K 调整和多路融合改善复杂问题召回。只有完成这些配对消融实验后，才能把 badcase 的下降归因于三阶段分片或具体检索模块。

完整逐条复核记录见 [可读版 badcase 结果](/Users/a1234/RAG_DB_silm/results/server0/badcase_reviewed_readable.json)。
