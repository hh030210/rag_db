# 维度挖掘与检索系统设计文档

> 本文档描述 RAG_DB_slim 项目中"维度挖掘"与"维度感知检索"两大模块的完整设计。
> 涵盖：维度如何从原始数据中生成、如何经过分阶段融合优化、如何打标入库、如何在查询时驱动混合检索。

---

## 目录

1. [系统全景图](#1-系统全景图)
2. [完整流水线架构](#2-完整流水线架构)
3. [模块一：维度挖掘 (Dimension Mining)](#3-模块一维度挖掘-dimension-mining)
   - 2.1 数据加载与聚类采样
   - 2.2 LLM 归纳候选维度
   - 2.3 迭代优化（诊断 → 分阶段融合 → 兜底删除 → 冗余检测）
   - 2.4 LLM 服务接口
4. [模块二：标签生成 (Tag Generation)](#4-模块二标签生成-tag-generation)
   - 4.1 批量多维度抽取
   - 4.2 兜底关键词策略
5. [模块三：维度感知检索 (Dimension-Aware Retrieval)](#5-模块三维度感知检索-dimension-aware-retrieval)
   - 5.1 索引构建
   - 5.2 QueryParser 查询意图解析
   - 5.3 向量检索（Qdrant ANN）
   - 5.4 标签软匹配
   - 5.5 多路融合排序
6. [模块四：约束过滤检索 (Dimension Constraint Retrieval)](#6-模块四约束过滤检索-dimension-constraint-retrieval)
7. [增量数据处理](#7-增量数据处理)
   - 7.1 增量写入核心策略
   - 7.2 幂等性保证
   - 7.3 多数据集增量合并
   - 7.4 批量处理与断点续传
   - 7.5 空列自动清理
8. [关键设计决策](#8-关键设计决策)
9. [文件索引](#9-文件索引)

---

## 1. 系统全景图

```
原始文档
   │
   ▼
┌─────────────────────────────────────────┐
│  Step 1: 聚类采样                        │
│  KMeans 聚类 → 质心+边界采样 → 轻量代表性样本│
└──────────────────┬──────────────────────┘
                   ▼
┌─────────────────────────────────────────┐
│  Step 2: LLM 归纳候选维度                  │
│  PROMPT_GENERATE_CANDIDATES → V_cand     │
└──────────────────┬──────────────────────┘
                   ▼
┌─────────────────────────────────────────┐
│  Step 3: 迭代优化                         │
│  ┌────────────────────────────────────┐  │
│  │ Part A: 单维度诊断                   │  │
│  │   cov < TH_COV → 分阶段融合          │  │
│  │   dis < TH_DIS → 分阶段融合          │  │
│  │   融合失败 → 兜底 DELETE            │  │
│  └──────┬─────────────────────────────┘  │
│  ┌──────▼─────────────────────────────┐  │
│  │ Part B: 双维度冗余检测               │  │
│  │   R_diff < TH_DIFF → LLM MERGE     │  │
│  └────────────────────────────────────┘  │
│  → V_core（核心维度集）                   │
└──────────────────┬──────────────────────┘
                   ▼
┌─────────────────────────────────────────┐
│  Step 4: 标签生成                        │
│  LLM 批量抽取 + 关键词兜底 → tags_output  │
└──────────────────┬──────────────────────┘
                   ▼
┌─────────────────────────────────────────┐
│  Step 5: 索引构建                        │
│  倒排索引 + 标签向量 + 维度元数据          │
└──────────────────┬──────────────────────┘
                   ▼
          ┌────────┴────────┐
          ▼                 ▼
   ┌────────────┐    ┌──────────────┐
   │检索模式 A   │    │检索模式 B    │
   │混合检索     │    │约束过滤检索  │
   │向量+标签融合│    │维度约束+SQL │
   └────────────┘    └──────────────┘
```

   └────────────┘    └──────────────┘
```

---

## 2. 完整流水线架构

### 2.1 流水线概述

**主控文件**: `pipeline_qdrant.py`
**目标**: 将原始文本数据经过智能分片、维度抽取与打标，最终存入 MySQL (RDB) + Qdrant (VecDB) 的混合存储结构，支持维度感知的语义检索。

### 2.2 环境依赖

```bash
pip install mysql-connector-python qdrant-client FlagEmbedding scikit-learn scipy tqdm
```

### 2.3 六步流水线架构

```
┌─────────────────────────────────────────────────────────────┐
│                     输入: 原始文本文件                        │
│                   (.txt / .md / .json)                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 1: 初始化数据库                                        │
│  ├─ MySQL MainIndex 表 (重建/创建)                          │
│  └─ Qdrant Collection (Schema + 索引)                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 2: 分片处理                                            │
│  └─ integrated_chunker.py                                   │
│      Round 1: 结构边界拆分 + LLM 推荐 l_min/l_max           │
│      Round 2: PPL 去噪 + 语义切分                            │
│      Round 3: IG 贪心融合 + 小块合并                         │
│  输出: output_chunks/all_chunks_chunks.json                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 3: 分片入库                                            │
│  ├─ 强制重建 MySQL MainIndex 表                             │
│  └─ 批量写入: doc_id, corpus_id, doc_text, profile_json     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 4: 维度抽取与打标                                      │
│  ├─ 4a 维度挖掘 (聚类采样 → LLM归纳 → 迭代优化)            │
│  ├─ 4b 添加维度列到 MySQL                                  │
│  ├─ 4c LLM 批量打标签 (断点续传)                           │
│  └─ 4d 标签回写 MySQL                                      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 5: 索引构建 + Qdrant 迁移                             │
│  ├─ 5a 构建检索索引 (倒排索引 + 维度元数据 + 标签向量)       │
│  └─ 5b 全量迁移到 Qdrant (含维度列)                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     输出: 完整检索系统                         │
│  MySQL (维度标签) + Qdrant (向量+维度) + 外部索引文件          │
└─────────────────────────────────────────────────────────────┘
```

### 2.4 命令行接口

```bash
# 完整流程（一次性执行所有步骤）
python pipeline_qdrant.py --input data/ --step all

# 仅执行特定步骤
python pipeline_qdrant.py --input data/ --step 4 --force        # 强制重建写入
python pipeline_qdrant.py --input data/ --step 4                # 增量写入
python pipeline_qdrant.py --step 5 --dataset medical             # 医学数据集标签生成
python pipeline_qdrant.py --step 6                               # 索引构建 + 迁移
```

### 2.5 数据流向

```
原始数据文件 → Step 2 (分片) → output_chunks/
                                    │
                                    ▼
Step 3 (入库) ← output_chunks/ → MySQL MainIndex
                                    │
                                    ▼
Step 4 (维度打标) → experiment_data/tags_output_{dataset}.json
                                    │
                                    ▼
Step 5 (索引) → experiment_data/inverted_index.json
              → experiment_data/dimension_metadata.json
              → experiment_data/tag_vectors.pkl
              → Qdrant Collection
```

### 2.6 配置文件

主要配置项（`db_config.py`）：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `embedding.model_path` | BGE-M3 模型路径 | `code/bge-m3` |
| `qdrant.collection` | Qdrant 集合名 | `dimension_aware_v1` |
| `qdrant.vector_dim` | 向量维度 | 1024 |
| `mysql.database` | 数据库名 | `rag_db` |
| `mysql.table` | 表名 | `MainIndex` |

---

## 3. 模块一：维度挖掘 (Dimension Mining)

**入口文件**: `code/dimension_generate.py`
**LLM 服务**: `code/llm_service.py`

### 2.1 数据加载与聚类采样

#### 2.1.1 迭代器加载 (避免深度分页问题)

通过 Milvus `query_iterator` 迭代拉取全量向量，避免传统 offset 分页越查越慢的问题：

```python
iterator = client.query_iterator(
    collection_name=Config.COLLECTION_NAME,
    filter="",
    output_fields=["id", "text", "vector"],
    batch_size=100
)
```

#### 2.1.2 两阶段加载策略

| 阶段 | 输出 | batch_size | 说明 |
|------|------|-----------|------|
| `_load_vectors_only` | `[ids], [vectors]` | 500 | 仅向量，轻量，用于聚类 |
| `_load_vectors_and_texts` | `[ids], [texts], [vectors]` | 100 | 含文本，用于迭代验证 |

#### 2.1.3 KMeans 质心 + 边界采样

```
原始向量集
    │
    ▼
KMeans(n_clusters=K_CLUSTERS=50)
    │
    ├── 质心样本 (N_CORE_SAMPLES=5): 距质心最近的点
    └── 边界样本 (N_BOUND_SAMPLES=5): 距质心最远的点
    │
    ▼
代表性文档 ID 集合 (约 500 个)
```

采样数量估算：50 簇 × (5 + 5) = 500 个文档，相比全量万条显著降低 LLM 调用成本。

---

### 2.2 LLM 归纳候选维度

**Prompt 模板**: `PROMPT_GENERATE_CANDIDATES`

```
你是一个专业的领域知识结构化专家。
请从以下文档集合中，归纳出能够全面描述该领域知识的所有关键维度（Dimension）。

要求：
1. 维度应具有领域代表性，涵盖该领域的主要信息轴
2. 每个维度的值应当是离散的、可枚举的
3. 优先识别高频共性维度，兼顾领域特殊性
4. 维度之间应尽量独立，避免重复

请直接输出维度列表，用中文逗号分隔：维度1, 维度2, 维度3, ...
```

LLM 返回逗号分隔的维度名列表（如 `"适宜人群, 疾病类别, 治疗方案"`），解析后存入 `V_cand_initial.json`。

---

### 2.3 迭代优化（诊断 → 分阶段融合 → 兜底删除 → 冗余检测）

迭代上限：`MAX_ITER = 3`。已判定为 KEEP 的维度加入 `verified_dims` 白名单，后续迭代跳过。

#### 2.3.1 单维度抽取验证

对每个维度，在验证集文本上逐条调用 `extract_dimension_value(text, dim)`，收集所有非空抽取结果 `extraction_results[dim]`。

#### 2.3.2 Part A：单维度诊断（覆盖率 & 辨识度）

**覆盖率** (Coverage)：

```
cov = 命中文本数 / 总文本数
TH_COV = 0.50
```

**辨识度/信息熵** (Discrimination)：

```
dis = entropy(value_counts / sum(value_counts))
TH_DIS = 1.0
```

#### 2.3.3 分阶段融合流程（核心改动）

不达标的维度（cov < TH_COV 或 dis < TH_DIS）进入"分阶段融合"流程：

```
不合格维度 A
    │
    ├──────────────────────────────────────┐
    ▼                                      │
Stage 1: 与"合格维度"融合                   │
    │  调用 merge_with_targets(              │
    │    candidate_targets = candidates_for_diff_check
    │  )                                    │
    │                                      │
    ├── MERGE ──────────────────────────────┤
    │  删除 A，加入 new_dimensions           │
    │  输出: [Stage1 融合] ✓                │
    │                                      │
    └── NOT_MERGE                          │
         │                                 │
         ▼                                 │
Stage 2: 与"其他不合格维度"融合              │
    │  调用 merge_with_targets(              │
    │    candidate_targets = other_failing   │
    │  )                                    │
    │                                      │
    ├── MERGE ──────────────────────────────┤
    │  删除 A 和被合并方 B，                 │
    │  加入 new_dimensions                   │
    │  输出: [Stage2 融合] ✓                │
    │                                      │
    └── NOT_MERGE                          │
         │                                 │
         ▼                                 │
兜底: 直接 DELETE                           │
      输出: [兜底删除] ✓                    │
      不再调用 LLM，不再强行融合             │
```

**关键规则**：
- Stage 1 优先：不合格维度先尝试与**合格维度**融合，防止"两个都不行硬凑"。
- Stage 2 降级：Stage 1 失败后，再与其他不合格维度融合。
- 兜底删除：两阶段都失败，**直接 DELETE**，不再调用 LLM 自选动作。
- 被合并方同步删除：Stage 2 融合成功时，`merge_target`（被合并方）也加入 `dims_to_remove`，避免出现"新增了合并维度但旧方还在"的悬空状态。

#### 2.3.4 Part B：双维度冗余检测

仅在**合格维度**之间进行两两比较，使用两个相似度指标：

**语义相似度**（Static，公式 2-5）：

```python
dim_vectors = encoder.encode(check_list)['dense_vecs']
sim_def_matrix = cosine_similarity(dim_vectors)
sim_def = sim_def_matrix[i][j]
```

**数据相似度**（Dynamic，公式 2-6，Jaccard）：

```python
vals_a = extraction_results[dim_a]
vals_b = extraction_results[dim_b]
sim_data = Jaccard(vals_a, vals_b)
```

**冗余度 & 独立性得分**（公式 2-7、2-8）：

```
redundancy_score = min(sim_def, sim_data)
R_diff = 1.0 - redundancy_score
TH_DIFF = 0.3
if R_diff < TH_DIFF → 触发 LLM 冗余处理
```

触发冗余处理后，调用 `optimize_dimension(issue_type="语义/数据冗余")`，若 LLM 返回 MERGE，则 A 和 B 同时被删除，合并为新维度。

---

### 2.4 LLM 服务接口

| 方法 | 功能 | 返回 |
|------|------|------|
| `generate_candidate_dimensions(docs)` | LLM 归纳候选维度 | `List[str]` |
| `extract_dimension_value(text, dim)` | 单维度单值抽取 | `str \| None` |
| `optimize_dimension(dim, issue, metric, samples)` | 维度优化决策 | `{action, reasoning, new_dimensions}` |
| `merge_with_targets(dim, issue, metric, samples, candidates)` | 分阶段融合决策 | `{action, merge_target, reasoning, new_dimensions}` |
| `extract_batch_dimensions(text, dims)` | 批量多维度抽取 | `{dim: [vals]}` |
| `extract_keywords_fallback(text)` | 兜底关键词抽取 | `List[str]` |
| `parse_query_intent(query, dims, enum_map, schema_fields)` | 查询意图解析 | `{dim: [vals]}` |

---

## 4. 模块二：标签生成 (Tag Generation)

**入口文件**: `code/tag_generate.py`

### 3.1 批量多维度抽取

对每篇文档，调用 `extract_batch_dimensions(text, dims)`，通过 `PROMPT_EXTRACT_BATCH` 让 LLM 一次性从文本中抽取多个维度的值：

```python
# 返回格式
{
    "适宜人群": ["儿童", "成人"],
    "疾病类别": ["呼吸道疾病"],
    "治疗方案": []
}
```

超过 15 个维度时分批调用，避免 Prompt 过长。

### 3.2 兜底关键词策略

若批量抽取全部无结果（`has_valid_content = False`），调用 `extract_keywords_fallback(text)` 提取 3-5 个关键词存入 `"其他"` 维度，保证每篇文档至少有标签输出。

---

## 5. 模块三：维度感知检索 (Dimension-Aware Retrieval)

**入口文件**: `code/dimension_search.py`

### 5.1 索引构建

索引构建（`pipeline_qdrant.py` Step 6 的一部分）从 `tags_output.json` 合并所有数据集标签，构建三类索引：

#### 4.1.1 倒排索引 (Inverted Index)

```json
{
  "适宜人群": {
    "儿童": ["doc_001", "doc_015", ...],
    "成人": ["doc_003", "doc_022", ...]
  },
  "疾病类别": { ... }
}
```

#### 4.1.2 维度元数据 (Dimension Metadata)

```json
{
  "适宜人群": {
    "is_enum": false,
    "value_count": 42,
    "values": ["儿童", "成人", "老人", ...]
  }
}
```

#### 4.1.3 标签向量 (Tag Vectors)

对每个维度的所有取值，用 BGE-M3 编码为向量，存入 `tag_vectors.pkl`（pickle），用于检索时的软匹配：

```python
embeddings = encoder.encode(val_list, return_dense=True)['dense_vecs']
tag_vectors[dim] = {"values": val_list, "vectors": embeddings}
```

#### 4.1.4 正向标签索引 (Forward Index)

从倒排索引反向构建：`doc_id → {dim: [val1, val2]}`，用于快速查找文档的维度标签。

---

### 5.2 QueryParser 查询意图解析

**入口文件**: `code/query_parser.py`

```python
result = parser.parse(qid, query_text, dim_meta)
# 返回: {"适宜人群": ["儿童"], "疾病类别": ["发烧"]}
```

支持 Enum 维度（提供候选值列表）和 Open 维度（无预定义枚举）。查询结果自动缓存到 `query_cache.json`，避免重复 LLM 调用。

---

### 5.3 向量检索（Qdrant ANN）

对查询文本编码后，在 Qdrant 中执行 ANN 检索，召回 Top-200 候选：

```python
from qdrant_client import QdrantClient
query_vec = encoder.encode([query_text])['dense_vecs']
results, _ = client.search(
    collection_name=collection,
    query_vector=query_vec[0],
    limit=200
)
```

---

### 5.4 标签软匹配

对查询约束中的每个值，在倒排索引中匹配：

| 维度类型 | 匹配策略 | 得分 |
|---------|---------|------|
| Enum（枚举） | 严格字面匹配 | 1.0 |
| Open（开放） | BGE-M3 向量余弦相似度 > 0.65 | similarity |
| 字面完全一致 | 额外给 1.0（优先级最高） | 1.0 |

软匹配得分按**维度级别去重**（同维度只取最高分），防止近义词重复加分。

---

### 5.5 多路融合排序

在向量召回的 Top-200 候选池内，按两路得分重新排序。

#### 5.5.1 Score 融合（加权和）

```
final_score = vec_score + α × tag_score
```

`α`（默认 0.2）控制标签权重，向量分始终占主导。

#### 5.5.2 RRF 融合（倒数排名融合）

```
final_rrf = 1/(K + vec_rank) + α × 1/(K + tag_rank)
K = 60（RRF 平滑常数）
```

RRF 优势：可召回向量排名不高但标签命中强的文档，实现跨两路的互补融合。

#### 5.5.3 结果输出

每条结果包含完整可解释信息：

```json
{
  "rank": 1,
  "doc_id": "doc_042",
  "final_score": 0.7834,
  "vec_score": 0.7421,
  "tag_score": 0.2067,
  "vec_rank": 3,
  "tag_rank": 5,
  "vec_contrib": 0.0159,
  "tag_contrib": 0.0032,
  "tags": {"适宜人群": ["儿童"], "疾病类别": ["发烧"]},
  "evidence": ["[适宜人群] Query '儿童' → 软命中 '儿童' (0.92)"],
  "doc_text": "儿童感冒发热，可使用布洛芬混悬液..."
}
```

---

## 6. 模块四：约束过滤检索 (Dimension Constraint Retrieval)

**入口文件**: `code/dimension_retriever.py`

与模块三的"混合检索"不同，模块四处理**显式维度约束**场景（如用户在 UI 上点了"人群=老人、景区=故宫"），不需要向量语义扩展：

```
用户约束 {dim: [val1, val2]}
    │
    ▼
DimensionConstraintBuilder.build_sql_filter()
    │
    ▼
SELECT * FROM table
WHERE dim_适宜人群_1 IN ('儿童')
  AND dim_疾病类别_1 IN ('发烧')
LIMIT top_k
    │
    ├── pure_filter: 直接返回过滤结果
    └── hybrid:      过滤 doc_id → Qdrant 向量精排
```

**支持两种模式**：

| 模式 | 说明 | 适用场景 |
|------|------|---------|
| `pure_filter` | 仅 SQL 维度过滤 | 精确维度筛选 |
| `hybrid` | SQL 过滤 + 向量精排 | 维度约束 + 语义排序 |

---

## 7. 增量数据处理

增量数据处理功能主要在 **Step 3（分片入库）** 和 **Step 6（索引构建）** 中实现，确保新增数据可以幂等地合并到已有系统中。

### 7.1 增量写入核心策略

#### 7.1.1 两种写入模式

| 模式 | 参数 | 行为 |
|------|------|------|
| **增量写入（默认）** | `force=False` | 保留已有表，新增/更新记录 |
| **强制重建** | `force=True` | 先 `DROP TABLE`，再重建全量写入 |

```python
def step4_write_to_rdb(self, chunks_file: str = None, corpus_id: str = "", force: bool = False):
    if not force:
        # 检查表是否存在
        cur.execute(f"SHOW TABLES LIKE %s", (table,))
        table_exists = cur.fetchone() is not None
        if table_exists:
            print(f"  保留已有表: {database}.{table}（Step 4 将以增量模式写入）")
            return {"status": "exists", "message": "保留表"}
    # force=True: 强制删除重建
    cur.execute(f"DROP TABLE IF EXISTS `{table}`")
```

#### 7.1.2 增量写入返回值

```python
inserted, updated, failed = self._upsert_chunks(all_chunks)
print(f"  -> 新增 {inserted} 条，更新 {updated} 条，失败 {failed} 条")
```

### 7.2 幂等性保证

采用 **`INSERT ... ON DUPLICATE KEY UPDATE`** 语法保证幂等性：

```sql
INSERT INTO `MainIndex` (`doc_id`, `corpus_id`, `doc_text`, `profile_json`)
VALUES (%s, %s, %s, %s)
ON DUPLICATE KEY UPDATE
    `doc_text` = VALUES(`doc_text`),
    `profile_json` = VALUES(`profile_json`)
```

| 场景 | 行为 |
|------|------|
| 已有 `doc_id` | 仅更新 `doc_text` 和 `profile_json`，`corpus_id` 保持不变 |
| 新 `doc_id` | 插入全新记录 |
| 不同 `corpus_id` 的已有记录 | 完全保留，不覆盖 |

### 7.3 多数据集增量合并

#### 7.3.1 标签文件命名约定

系统支持按数据集隔离输出：

- `tags_output.json` — 默认数据集
- `tags_output_{dataset}.json` — 命名数据集（如 `tags_output_medical.json`）

#### 7.3.2 扫描合并逻辑

```python
tags_files = []
tags_files.append(experiment_data_dir / "tags_output.json")
tags_files.extend(experiment_data_dir.glob("tags_output_*.json"))

for f in available:
    with open(f, "r", encoding="utf-8") as fp:
        data = json.load(fp)
    name = f.stem.replace("tags_output", "").lstrip("_") or "default"
    merged_doc_data.update(data)  # 增量合并到同一结构
```

#### 7.3.3 覆盖率感知的维度过滤

```python
MIN_COV = 0.05  # 任一数据集覆盖率 ≥5% 即保留

for dim, val_set in dim_value_sets.items():
    per_dataset_coverage = {
        ds: dim_dataset_doc_counts[dim][ds] / dataset_doc_counts[ds]
        for ds in dataset_doc_counts
    }
    max_cov = max(per_dataset_coverage.values())
    if max_cov >= MIN_COV:
        dims_to_vectorize.append((dim, sorted(list(val_set))))
```

### 7.4 批量处理与断点续传

#### 7.4.1 分批提交策略

```python
BATCH_SIZE = 100
for start in range(0, len(batch), BATCH_SIZE):
    chunk_batch = batch[start:start + BATCH_SIZE]
    try:
        cur.executemany(sql, chunk_batch)
        conn.commit()
    except Exception:
        # executemany 失败时降级为逐条插入
        for row in chunk_batch:
            cur.execute(sql, row)
            conn.commit()
```

- 每 **100 条** 为一批次提交
- `executemany` 失败时自动降级为逐条插入，保证可靠性

### 7.5 空列自动清理

维度标签回写后，系统自动清理全 NULL 的维度列：

```python
def _cleanup_empty_dim_columns(self, cur, conn, table: str) -> int:
    """删除全是 NULL 的维度列"""
    cur.execute(f"SHOW COLUMNS FROM `{table}`")
    dim_cols = [c for c in all_cols if c.startswith('dim_')]

    for col in dim_cols:
        cur.execute(f"""
            SELECT COUNT(*) FROM `{table}`
            WHERE `{col}` IS NOT NULL AND `{col}` != ''
        """)
        count = cur.fetchone()[0]
        if count == 0:
            cur.execute(f"ALTER TABLE `{table}` DROP COLUMN `{col}`")
            conn.commit()
    return cleaned
```

### 7.6 增量数据流总结

```
原始数据文件
    │
    ▼
[Step 3] 增量写入 MySQL
    ├── 表存在 → INSERT ON DUPLICATE KEY UPDATE（幂等）
    ├── 表不存在 → 自动创建
    └── corpus_id 隔离，互不覆盖
    │
    ▼
[Step 4] 维度挖掘 + 标签生成
    ├── 按数据集隔离输出（tags_output_{dataset}.json）
    └── 空列自动清理
    │
    ▼
[Step 6] 索引构建 + 迁移
    ├── 扫描所有 tags_output_*.json
    ├── 相同维度值合并到同一倒排索引
    └── 全量数据迁移到 Qdrant
```

### 7.7 增量处理关键特性总结

| 特性 | 实现方式 |
|------|----------|
| **幂等写入** | `INSERT ... ON DUPLICATE KEY UPDATE` |
| **多数据集隔离** | `tags_output_{dataset}.json` 命名约定 |
| **批量处理** | 每 100 条为一批，失败自动降级 |
| **空列清理** | 自动检测并删除全 NULL 的维度列 |
| **覆盖率过滤** | 跨数据集取最大覆盖率，≥5% 即保留 |
| **增量索引合并** | 扫描所有 `tags_output*.json` 并合并倒排索引 |

---

## 8. 关键设计决策

### 8.1 为什么用聚类采样而不是全量调用 LLM？

全量验证 N 个维度的覆盖率/辨识度需要 N × M 次 LLM 调用（M = 验证集大小）。KMeans 采样将候选集压缩至约 500 条代表性文档，LLM 调用量从 O(N×10000) 降至 O(N×500)。

### 8.2 为什么融合要分阶段而不是直接让 LLM 自由决定？

原版 `optimize_dimension` 在 MERGE 时不限制目标选择，LLM 可能将两个"都不达标"的维度强行合并，效果等于"差+差=差"。分阶段强制约束：
- Stage 1：必须从合格维度中选目标（质量基准）。
- Stage 2：降级到不合格维度（次优匹配）。
- 兜底：两阶段都失败 → DELETE（不再强融）。

### 8.3 为什么冗余检测只在合格维度之间进行？

冗余检测的假设是两个维度都**有价值**，只是彼此重叠。一个覆盖率 10% 的低质量维度与覆盖率 60% 的维度检测"冗余"是无意义的——低覆盖率维度本身就该走 Part A 的融合/删除流程。

### 8.4 为什么 Open 维度用向量软匹配而不是严格匹配？

开放维度（如"治疗方案"）的值空间不可枚举，用户查询的表述（"退烧"、"降体温"）可能与文档中的值（"退热"）语义相近但字面不同。向量相似度 > 0.65 作为软匹配阈值，在精确性和召回率之间取得平衡。

### 8.5 RRF 中 K=60 的含义

K 是 RRF 的平滑常数，K=60 意味着排名差异在前 60 位内影响较大，超过 60 名后贡献趋于 0。这是一个经验值，兼顾了 Top-200 召回池内各文档的区分度。

---

## 9. 文件索引

| 文件 | 角色 | 关键类/函数 |
|------|------|-----------|
| `code/dimension_generate.py` | 维度挖掘主流程 | `DimensionMiningSystem` |
| `code/llm_service.py` | 所有 LLM 调用 | `DimensionMiningWithQwen` |
| `code/tag_generate.py` | 标签批量生成 | `LabelGeneratorFull` |
| `code/dimension_search.py` | 混合检索（向量+标签融合） | `DimensionAwareSearch` |
| `code/dimension_retriever.py` | 约束过滤检索 | `DimensionRetriever`, `DimensionConstraintBuilder` |
| `code/query_parser.py` | 查询意图解析 | `QueryParser` |
| `code/index_builder.py` | 索引构建 | `IndexBuilder` |
| `pipeline_qdrant.py` | 端到端流水线编排 | `Pipeline` |
| `integrated_chunker.py` | 智能分片处理 | 三轮分片逻辑 |
| `experiment_data/V_cand_*.json` | 初始候选维度 | — |
| `experiment_data/V_core_*.json` | 优化后核心维度 | — |
| `experiment_data/tags_output*.json` | 文档维度标签（支持多数据集） | — |
| `experiment_data/inverted_index.json` | 倒排索引 | — |
| `experiment_data/tag_vectors.pkl` | 维度值向量 | — |
| `experiment_data/dimension_metadata.json` | 维度元数据 | — |
