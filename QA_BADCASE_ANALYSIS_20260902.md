# 2026-09-02 融合检索问答实验与 badcase 分析

## 1. 实验范围

本次实验使用全量问答集，共 436 条，其中 435 条问题有效、1 条为空问题。检索库为当前服务器上的 Qdrant `unified_corpus`，包含 309 个 chunk；11 个 `dim_*` 维度字段已经回写，其中 250/309 个 chunk 至少有一个非空维度标签。

本轮采用融合检索：语义向量检索与 Qdrant payload 中的维度标签向量检索共同召回，使用 adaptive 融合和 score 重排，Top-K 为 5。未启用 Query Expansion，以便直接观察当前维度标签对融合检索的影响。问答模型为 qwen-plus；检索轨迹最多保存每个 chunk 2000 字符，但实际生成答案时每个 chunk 仍传入前 500 字符；相邻请求间隔为 2 秒。

## 2. 总体结果

| 指标 | 结果 |
|---|---:|
| 总问题数 | 436 |
| 有效问题数 | 435 |
| 规则初筛候选 badcase | 301/435（69.2%） |
| 二次复核为最终 badcase | 179/435（41.1%） |
| 二次复核为可接受 | 121/301（40.2%） |
| 二次复核不确定 | 1/301（0.3%） |
| 自动 Hit@5 | 246/435（56.6%） |
| 自动未命中 | 189/435（43.4%） |
| 复核确认检索 miss | 66/301 |
| 复核确认检索 hit 且答案仍为 badcase | 113/301 |

最终 badcase 的错误类型如下。这里的分类来自 Qwen 二次复核，不是人工金标准。

| 错误类型 | 数量 | 占最终 badcase |
|---|---:|---:|
| 检索证据缺失 | 58 | 32.4% |
| 答案不完整 | 48 | 26.8% |
| 无依据拒答 | 38 | 21.2% |
| 事实错误或对象混淆 | 35 | 19.6% |
| 合计 | 179 | 100.0% |

## 3. 与此前纯语义检索结果的对比

此前同一问答集的纯语义检索结果为：自动 Hit@5 为 272/435，Qwen 二次复核最终 badcase 为 136 条、可接受为 146 条、确认检索 miss 为 38 条。本次切换到当前维度标签融合后，结果如下：

| 指标 | 纯语义检索 | 当前维度融合 | 变化 |
|---|---:|---:|---:|
| 自动 Hit@5 | 272 | 246 | -26 |
| 自动未命中 | 163 | 189 | +26 |
| 最终 badcase | 136 | 179 | +43 |
| 可接受候选 | 146 | 121 | -25 |
| 复核确认检索 miss | 38 | 66 | +28 |

在当前实现和本次测试条件下，维度融合没有带来问答质量提升，反而使检索 miss 和最终 badcase 增加。该对比受 LLM 多次调用的随机性和运行时间影响，不能单凭一次实验证明维度方法本身一定无效，但足以说明当前维度分支的接入方式需要优化后再做论文结论。

## 4. badcase 原因分析

### 4.1 维度分支过宽，且缺少景区硬约束

本轮 435 条有效问题都返回了 100 条维度候选和 20 条语义候选；最终 Top-1 的来源全部是 `dim+sem`，说明维度分支持续参与排序。自适应维度权重在 0.4105–0.4887 之间，平均为 0.4699，并没有在维度证据较弱时明显关闭维度分支。

查询约束平均包含多个维度，最常见是 6–8 个维度。以“长陵和定陵分别是哪位皇帝的陵寝”为例，约束中同时出现了大量历史、地理、功能、建筑、宗教和游览服务标签，其中还出现与问题无关的“永定城”“西湖三绝中的长桥”等值。这样会把跨景区、跨主题标签一起送入维度向量匹配，维度分支不再是精确约束，而变成一个宽泛的语义召回器。

当前 11 个核心维度中没有单独的“景区名称”确定性字段，查询也没有通过 payload 对景区做硬过滤。因此，多景区共用 collection 时，问题中的景区名没有成为不可违反的边界。典型结果是询问十三陵，却把龙门石窟、颐和园、南孔庙等 chunk 排到 Top-K。

### 4.2 维度向量匹配存在维度偏置和标签噪声

当前 Top-1 维度分布为：游览服务 132 条、历史沿革 89 条、文化象征 59 条、地理位置 44 条、景观特征 38 条，其余维度较少。Top-5 中游览服务出现 573 次，是最明显的高频维度。它在标签覆盖率上只有 45/309，但却频繁进入 Top-1，说明标签向量相似度、标签数量或重排分数之间存在偏置，不能把“向量相似”直接当作“问题维度严格匹配”。

因此，当前流程中的维度匹配更接近“跨维度标签语义相似召回”，而不是“先确定问题维度，再在同一维度内匹配标签”。这会造成看似有标签命中、实际答非所问的 badcase。

### 4.3 检索 miss

复核确认的 66 条检索 miss 主要集中在：实体别名没有统一、问题包含多个子问题、问题需要精确数值或顺序、以及跨景区干扰。代表性问题包括：

| 编号 | 现象 | 结果 |
|---:|---|---|
| 14 | 问神道石像生的数量和排列顺序，却召回龙门石窟、张家界内容 | 系统拒答 |
| 15 | 问十三陵陵寝布局规则，Top-K 没有布局规则证据 | 系统拒答 |
| 22 | 问石像生动物种类及寓意，Top-K 没有相关实体 | 系统拒答 |
| 33 | 问棂星门、龙凤门、火焰牌坊的关系，召回无关景区 | 系统拒答 |
| 43 | 问大红门两侧建筑及现状，Top-K 主要是颐和园、西湖等内容 | 系统拒答 |

需要注意，自动规则判断为未命中的 189 条中，有 129 条在二次复核时被判定为实际命中。这表明当前标准证据文本近似匹配过于严格，存在证据文本改写、切片边界不同或 Top-K 已包含相关内容但没有完整包含标准证据的情况。评测指标本身也需要改进，最好为每道题提供标准 chunk ID 或证据句 span。

### 4.4 已检索到证据，但答案生成仍失败

113 条最终 badcase 的检索判断为 hit，说明问题不全在召回。主要有三类：

- 答案不完整：48 条。常见于“分别”“列举两个”“至少三类”“从哪里开始”等槽位要求，模型只回答了部分内容。
- 无依据拒答：38 条。上下文已有答案，模型仍输出“文中未说明”，说明生成阶段没有正确定位证据，或被过强的拒答规则影响。
- 事实错误/对象混淆：35 条。常见于多个景区同名建筑、问题中的“景区”未明确指代、以及上下文中同时出现多个候选实体。

这里最值得优先检查的是上下文截断：结果文件保存每个 chunk 最多 2000 字符，但生成答案只使用每个 chunk 的前 500 字符。许多长 chunk 的关键事实位于后半段，因而会出现“检索轨迹看起来命中，但模型实际没有看到关键句”的现象。

代表性生成问题包括：

| 编号 | 类型 | 主要原因 |
|---:|---|---|
| 1 | 不完整 | 已召回长陵、定陵信息，但只回答明朝，遗漏 1409 年和 1584 年 |
| 17 | 不完整 | 只回答定陵，遗漏长陵和永陵的规模比较 |
| 51 | 事实错误 | 上下文给出石牌坊建于 1540 年，模型却拒答 |
| 61 | 事实错误 | 已召回讲解预约、费用和语言信息，模型仍称没有相关信息 |
| 93 | 对象混淆 | 问联票时回答成颐和园，未锁定当前问题对应景区 |
| 108 | 事实错误 | 已召回儿童活动免费信息，却回答儿童不免费 |

## 5. 优化方案

### P0：先修正景区和实体边界

1. 对每个 chunk 独立写入可审计的景区名称、文档标题、建筑/景点实体等 payload 字段。景区名称应由来源或标题确定性生成，不依赖每个 chunk 的 LLM 自由抽取。
2. 查询侧先抽取景区、实体、问题类型和子问题；景区名称做精确/别名匹配。高置信度命中时，对 Qdrant 使用硬过滤，禁止其他景区 chunk 进入候选。
3. 建立建筑别名和简称表，例如“神路/神道”“祾恩殿/金丝楠木大殿”等，并把别名匹配结果记录到检索轨迹。

### P1：把维度匹配改成“同维度精确优先、向量兜底”

1. 维度抽取只输出有限 schema 中真正与问题相关的 1–3 个维度，不要为一个问题默认激活 6–11 个维度。
2. 在同一维度内部先做标准化后的精确匹配和别名匹配；只有没有精确命中时，才启用该维度的向量标签召回。
3. 对向量标签设置最低相似度和与第二名的 margin；低于阈值时不进入融合，不要把弱相似标签当作约束证据。
4. 维度候选必须保留“维度名、标签、相似度、匹配来源、证据文本”。外层融合只接收纯维度候选，避免维度内部融合结果再次被宽泛融合。

### P1：调整融合和重排

1. 只有在存在高置信度景区/实体/标签证据时才提高维度权重；没有可靠维度证据时让语义检索占主导。
2. 对维度分数和语义分数分别归一化，再结合景区一致性、实体一致性和必答槽位覆盖率重排。
3. 限制维度分支能进入最终 Top-K 的数量，至少保留一个与问题实体和核心槽位高度匹配的语义 chunk。
4. 对“分别、列举、至少、多少、顺序、比较”等问题先拆成必答槽位，重排时优先保证槽位覆盖，而不是只看一个总分。

### P2：修正答案生成与评测

1. 将生成上下文从每 chunk 前 500 字改为证据句窗口，或提高到 1000–2000 字；优先传入包含查询实体、数字和标签证据的句子。
2. 生成前对 Top-K 做相邻 chunk 去重和证据句抽取，避免把大量无关文本挤占上下文。
3. 让模型逐项填写问题槽位，并要求数字、人物、时间和对象只能来自证据；证据存在时禁止直接输出“文中未说明”。
4. 重新构造评测集的 `evidence_chunk_ids`，并补充证据句 span，分别计算 Recall@5、MRR、证据支持率、必答槽位覆盖率和答案正确率。
5. 重新做四组消融：纯语义、纯精确/别名标签、精确/别名标签+语义、向量标签+语义。每组使用相同问答调用、相同 Top-K 和相同随机参数。

## 6. 结果文件

- [完整逐题复核结果](results/badcase_fusion_tags_reviewed_20260902.jsonl)：436 条，包含问题、检索轨迹、答案、自动标签和二次复核结果。
- [最终 badcase 精简列表](results/badcase_fusion_tags_final_20260902.jsonl)：180 条，包含 179 条有效问答 badcase 和 1 条空问题输入异常。
- [问答自动初筛汇总](results/badcase_fusion_tags_20260902_summary.json)
- [badcase 二次复核汇总](results/badcase_fusion_tags_reviewed_20260902_summary.json)

本报告中的“最终 badcase”包含 Qwen 二次复核结论；其中 1 条不确定记录和 1 条空问题输入异常应单独人工确认，不应直接作为模型能力结论。

## 7. 上下文截断修复状态

针对本报告发现的“检索轨迹保存较长文本、生成模型只看到前 500 字”的问题，已在本地问答流程中完成修复：

- 默认情况下，问答模型使用完整的 `chunk_text_full`，不再按 500 字截断。
- 终端展示长度与模型输入长度已经完全分离，展示用的 300 字不会影响模型上下文。
- 如果调用方为了控制上下文显式设置长度上限，程序会优先定位问题相关证据窗口，并在找不到匹配词时保留文本首尾；不会再无条件只取文本开头。
- 每条问答结果新增上下文审计信息，包括原始字符数、实际提供字符数和是否发生截断，便于复现实验和核查证据是否真正传给模型。

本报告第 1–6 节的统计是修复前实验结果；要获得修复后的新指标，需要使用相同检索配置重新执行问答实验并与本报告结果对比。
